#!/usr/bin/env python
"""Standalone offline augmentation for fisheye camera-only 3D detection data."""

from __future__ import annotations

import argparse
import copy
import hashlib
import json
import math
import shutil
from collections import Counter
from concurrent.futures import ProcessPoolExecutor
from dataclasses import dataclass
from pathlib import Path
from typing import Any, Dict, List, Mapping, MutableMapping, Optional, Sequence, Tuple
import os
import cv2
import numpy as np


IMAGE_DIRS: Tuple[str, ...] = ("image0", "image1", "image2", "image3")
METADATA_DIRS: Tuple[str, ...] = ("camera_config", "position", "result")

LIDAR_MIRROR = np.diag([1.0, -1.0, 1.0, 1.0])
CAMERA_X_MIRROR = np.diag([-1.0, 1.0, 1.0, 1.0])

TARGET_TAIL_CLASSES = frozenset({"pedestrian", "bicycle", "motor","stopper"})
RARE_CLASSES = frozenset({"animal", "bus", "construction_vehicle", "traffic_cone"})
TAIL_CLASSES = frozenset({"truck", "trash_bin", "sign", "barrier"})
LONG_TAIL_CLASSES = RARE_CLASSES | TAIL_CLASSES
PARALLEL_WORKERS = 6

DEFAULT_TARGET_TOTAL_FRAMES = 500 #150000
DEFAULT_TRAIN_VAL_SPLIT = {
    "train": [
        "20240123-204430_20240123-204443",
        "20240109-221455_20240109-221515",
        "20240113-214554_20240113-214610",
        "20240114-211200_20240114-211214",
        "20240116-200650_20240116-200702",
        "20240115-200914_20240115-200925",
        "20240126-110712_20240126-110730",
        "20240113-140119_20240113-140144",
        "20240113-193345_20240113-193400",
        "20240124-140216_20240124-140234",
        "20240123-115956_20240123-120009",
        "20240123-135435_20240123-135446",
        "20240109-143220_20240109-143239",
        "20240119-211930_20240119-211950",
        "20240112-210133_20240112-210146",
        "20240123-210358_20240123-210420",
        "20240115-110841_20240115-110857",
        "20240115-204313_20240115-204331",
        "20240126-135916_20240126-135928",
        "20240110-211111_20240110-211127",
        "20240114-210126_20240114-210144",
        "20240125-205703_20240125-205716",
        "20240106-184236_20240106-184257",
        "20240114-201926_20240114-201945",
        "20231219-110017_20231219-110041",
        "20240120-202817_20240120-202830",
        "20240116-225248_20240116-225301",
        "20240110-210307_20240110-210320",
        "20240120-191856_20240120-191915",
        "20240113-160230_20240113-160253",
        "20240118-200222_20240118-200247",
        "20240112-151122_20240112-151134",
        "20240124-211939_20240124-211949",
        "20240115-202202_20240115-202220",
        "20240124-215129_20240124-215145",
        "20240116-200710_20240116-200723",
        "20240118-195030_20240118-195048",
        "20240116-140713_20240116-140731",
        "20240111-204428_20240111-204448",
        "20231128-161035_20231128-161047",
        "20240125-224016_20240125-224033",
        "20240116-193440_20240116-193457",
        "20240118-211459_20240118-211520",
        "20240124-133622_20240124-133636",
        "20240118-213456_20240118-213516",
        "20240119-193038_20240119-193051",
        "20240120-200830_20240120-200843",
        "20240123-201012_20240123-201025",
        "20240118-134859_20240118-134919",
        "20240120-143850_20240120-143914",
        "20240116-205918_20240116-205942",
        "20240114-140108_20240114-140133",
        "20240105-160430_20240105-160442",
        "20240119-152757_20240119-152819",
        "20240120-205414_20240120-205428",
        "20240120-193621_20240120-193645",
        "20240118-195327_20240118-195338",
        "20240114-220537_20240114-220558",
        "20240117-200939_20240117-200952",
        "20240117-114348_20240117-114413",
        "20240122-141748_20240122-141801",
        "20240124-215522_20240124-215535",
        "20240119-193412_20240119-193426",
        "20240125-204043_20240125-204058",
        "20231214-180757_20231214-180808",
        "20240124-155041_20240124-155053",
        "20240112-145355_20240112-145410",
        "20240110-142531_20240110-142548",
        "20240122-202609_20240122-202620",
        "20240124-222951_20240124-223005",
        "20240116-193316_20240116-193336",
        "20240114-210514_20240114-210528",
        "20240116-203422_20240116-203444",
        "20240118-102743_20240118-102754",
        "20240125-145634_20240125-145649",
        "20240119-202540_20240119-202600",
        "20240114-142807_20240114-142823",
        "20240111-145920_20240111-145941",
        "20240125-203656_20240125-203709",
        "20240116-203846_20240116-203908",
        "20240111-224220_20240111-224242",
        "20240119-151630_20240119-151655",
        "20240118-210625_20240118-210641",
        "20240110-142507_20240110-142522",
        "20240119-151558_20240119-151609",
        "20240109-134447_20240109-134504",
        "20240111-150010_20240111-150028",
        "20240125-222020_20240125-222032",
        "20240112-204235_20240112-204248",
        "20240112-141310_20240112-141330",
        "20240125-211333_20240125-211355",
        "20240124-150535_20240124-150550",
        "20240111-135142_20240111-135158",
        "20240110-141727_20240110-141747",
        "20240113-162134_20240113-162155",
        "20240124-150639_20240124-150657",
        "20240124-141855_20240124-141905",
        "20240113-210511_20240113-210527",
        "20240123-142345_20240123-142356",
        "20240104-133937_20240104-133950",
        "20240109-145644_20240109-145659",
        "20240108-140928_20240108-140948",
        "20240115-135645_20240115-135656",
        "20240122-111845_20240122-111902",
        "20240122-150757_20240122-150816",
        "20240116-202147_20240116-202202",
        "20240113-162309_20240113-162334",
        "20240111-204001_20240111-204013",
        "20240125-223453_20240125-223511",
        "20240111-204805_20240111-204828",
        "20240125-202646_20240125-202708",
        "20240125-221500_20240125-221511",
        "20240124-140930_20240124-140946",
        "20240114-205932_20240114-205947",
        "20240116-204542_20240116-204605",
        "20240124-214836_20240124-214853",
        "20240119-113803_20240119-113826",
        "20240114-201640_20240114-201657",
        "20240114-205521_20240114-205535",
        "20240115-203735_20240115-203755",
        "20240116-104423_20240116-104449",
        "20240105-160531_20240105-160545",
        "20240113-191234_20240113-191257",
        "20240116-200608_20240116-200631",
        "20240124-155140_20240124-155154",
        "20240114-202032_20240114-202057",
        "20240114-104635_20240114-104647",
        "20240123-142005_20240123-142016",
        "20231128-161436_20231128-161453",
        "20240106-183918_20240106-183942",
        "20240125-223521_20240125-223536",
        "20240123-203649_20240123-203701",
        "20240115-195821_20240115-195836",
        "20240116-203557_20240116-203616",
        "20240120-152100_20240120-152118",
        "20240116-142849_20240116-142901",
        "20240112-141055_20240112-141115",
        "20240126-140328_20240126-140346",
        "20240118-102846_20240118-102857",
        "20240114-220611_20240114-220629",
        "20240116-202114_20240116-202136",
        "20240120-112823_20240120-112834",
        "20240116-224953_20240116-225017",
        "20240114-221415_20240114-221435",
        "20240109-110630_20240109-110642",
        "20240115-204933_20240115-204951",
        "20240119-193106_20240119-193121",
        "20240115-201424_20240115-201441",
        "20240114-213456_20240114-213511",
        "20240117-114620_20240117-114637",
        "20240125-203733_20240125-203745",
        "20240112-141241_20240112-141301",
        "20240119-140935_20240119-140953",
        "20240124-134907_20240124-134922",
        "20240126-110442_20240126-110503",
        "20240110-205908_20240110-205925",
        "20240118-211124_20240118-211147",
        "20240115-194020_20240115-194032",
        "20240122-205036_20240122-205056",
        "20240117-114955_20240117-115017",
        "20240119-150640_20240119-150651",
        "20240119-133518_20240119-133538",
        "20240115-200640_20240115-200651",
        "20240113-212942_20240113-212957",
        "20240124-134729_20240124-134740",
        "20240110-195242_20240110-195255",
        "20240111-135442_20240111-135455",
        "20240113-141345_20240113-141359",
        "20240119-193309_20240119-193326",
        "20240122-195656_20240122-195714",
        "20240123-135415_20240123-135430",
        "20240116-193934_20240116-193959",
        "20240125-201852_20240125-201914",
        "20240124-154249_20240124-154308",
        "20240113-212418_20240113-212442",
        "20240112-204015_20240112-204038",
        "20240122-194502_20240122-194515",
        "20240120-203514_20240120-203527",
        "20240113-162539_20240113-162559",
        "20240123-133904_20240123-133919",
        "20240113-193618_20240113-193641",
        "20240125-202002_20240125-202018",
        "20240115-201454_20240115-201508",
        "20240111-145815_20240111-145838",
        "20240117-114445_20240117-114509",
        "20240120-102707_20240120-102728",
        "20240110-201738_20240110-201757",
        "20240126-135357_20240126-135413",
        "20240307-200001_20240307-200016",
        "20240122-204003_20240122-204021",
        "20240115-210049_20240115-210109",
        "20240111-212011_20240111-212029",
        "20240116-194032_20240116-194053",
        "20240114-103243_20240114-103254",
        "20240115-202959_20240115-203018",
        "20240125-210105_20240125-210123",
        "20240122-194444_20240122-194503",
        "20240114-200936_20240114-200952",
        "20240115-135536_20240115-135601",
        "20240118-211355_20240118-211415",
        "20240113-140423_20240113-140442",
        "20240119-202854_20240119-202913",
        "20240125-142132_20240125-142151",
        "20240123-200336_20240123-200351",
        "20240116-135300_20240116-135323",
        "20240111-135605_20240111-135627",
        "20240124-144251_20240124-144310",
        "20240113-215756_20240113-215819",
        "20240118-211525_20240118-211545",
        "20240119-150607_20240119-150621",
        "20240118-104813_20240118-104833",
        "20231219-105558_20231219-105609",
        "20240113-195233_20240113-195252",
        "20240111-224810_20240111-224824",
        "20240111-213350_20240111-213406",
        "20240125-222116_20240125-222129",
        "20240113-140619_20240113-140644",
        "20240113-141215_20240113-141239",
        "20240124-215559_20240124-215610",
        "20240120-200412_20240120-200424",
        "20240112-163405_20240112-163424",
        "20240124-134907_20240124-134921",
        "20240126-111851_20240126-111902",
        "20240122-114049_20240122-114102",
        "20231127-155718_20231127-155731",
        "20240125-142523_20240125-142535",
        "20240114-202114_20240114-202129",
        "20240109-105722_20240109-105737",
        "20240120-113757_20240120-113811",
        "20240118-134531_20240118-134552",
        "20240115-200329_20240115-200342",
        "20240116-201319_20240116-201339",
        "20240115-135944_20240115-135956",
        "20240109-134259_20240109-134312",
        "20240118-102412_20240118-102425",
        "20240118-115530_20240118-115552",
        "20240117-210958_20240117-211013",
        "20240110-141635_20240110-141648",
        "20240123-141000_20240123-141017",
        "20240114-203137_20240114-203152",
        "20240119-135515_20240119-135527",
        "20240118-211325_20240118-211343",
        "20240116-195836_20240116-195857",
        "20240115-112356_20240115-112411",
        "20240113-140159_20240113-140212",
        "20240119-112001_20240119-112014",
        "20240125-154135_20240125-154150",
        "20240113-220559_20240113-220610",
        "20240125-212849_20240125-212859",
        "20240118-115635_20240118-115650",
        "20240112-231533_20240112-231552",
        "20240125-145520_20240125-145540",
        "20231214-163109_20231214-163126",
        "20240125-152004_20240125-152021",
        "20240125-150502_20240125-150527",
        "20240114-104439_20240114-104450",
        "20240110-210240_20240110-210254",
        "20240120-201246_20240120-201303",
        "20240116-204829_20240116-204842",
        "20240119-211105_20240119-211116",
        "20240115-200405_20240115-200420",
        "20240115-210114_20240115-210125",
        "20240120-153400_20240120-153418",
        "20231128-160936_20231128-160955",
        "20240118-104925_20240118-104936",
        "20240111-224108_20240111-224121",
        "20240124-140238_20240124-140253",
        "20240114-210613_20240114-210629",
        "20240119-111315_20240119-111325",
        "20240120-153320_20240120-153336",
        "20240115-134040_20240115-134052",
        "20240111-204711_20240111-204731",
        "20240120-115355_20240120-115412",
        "20240120-132212_20240120-132236",
        "20240104-102206_20240104-102223",
        "20240116-112627_20240116-112643",
        "20240115-135816_20240115-135831",
        "20240116-192905_20240116-192918",
        "20240111-150041_20240111-150056",
        "20240114-221656_20240114-221712",
        "20240118-114949_20240118-115004",
        "20240126-135823_20240126-135836",
        "20240110-110149_20240110-110207",
        "20240110-141816_20240110-141836",
        "20240114-103150_20240114-103209",
        "20240120-140859_20240120-140912",
        "20240120-194334_20240120-194348",
        "20240123-200725_20240123-200744",
        "20240118-102806_20240118-102817",
        "20240111-134336_20240111-134359",
        "20240118-214134_20240118-214145",
        "20240123-195010_20240123-195023",
        "20240117-204350_20240117-204403",
        "20240123-115926_20240123-115939",
        "20240113-215939_20240113-215958",
        "20240123-211131_20240123-211147",
        "20240112-141140_20240112-141151",
        "20240115-210008_20240115-210030",
        "20240118-211715_20240118-211736",
        "20240118-102436_20240118-102448",
        "20240111-224749_20240111-224805",
        "20240119-143449_20240119-143510",
        "20240116-112656_20240116-112715",
        "20240125-150543_20240125-150559",
        "20240111-204237_20240111-204250",
        "20240109-213425_20240109-213445",
        "20240111-204333_20240111-204352",
        "20240122-212053_20240122-212112",
        "20240111-213126_20240111-213139",
        "20240114-221025_20240114-221050",
        "20240116-142619_20240116-142638",
        "20240113-140223_20240113-140235",
        "20240119-193537_20240119-193550",
        "20240112-200622_20240112-200642",
        "20240125-221941_20240125-221953",
        "20240119-210529_20240119-210548",
        "20240113-134121_20240113-134137",
        "20240116-205211_20240116-205230",
        "20240112-205938_20240112-205956",
        "20240113-212815_20240113-212840",
        "20240112-203221_20240112-203234",
        "20240119-143404_20240119-143422",
        "20240118-205040_20240118-205050",
        "20240113-220025_20240113-220045",
        "20240119-113553_20240119-113613",
        "20240115-193923_20240115-193934",
        "20240115-110153_20240115-110210",
        "20240115-135722_20240115-135736",
        "20240112-201313_20240112-201327",
        "20240125-201432_20240125-201456",
        "20240115-200716_20240115-200736",
        "20240122-201300_20240122-201317",
        "20240117-204229_20240117-204241",
        "20240122-210710_20240122-210722",
        "20240119-212721_20240119-212734",
        "20231127-160022_20231127-160035",
        "20240110-202256_20240110-202316",
        "20240123-200131_20240123-200144",
        "20240115-140316_20240115-140326",
        "20240123-140816_20240123-140838",
        "20240123-135830_20240123-135844",
        "20240117-204742_20240117-204758",
        "20231128-160908_20231128-160926",
        "20240122-201324_20240122-201345",
        "20240104-102653_20240104-102707",
        "20240109-145214_20240109-145229",
        "20240113-141835_20240113-141855",
        "20240126-112210_20240126-112235",
        "20240122-205820_20240122-205842",
        "20240119-152834_20240119-152857",
        "20240123-134116_20240123-134132",
        "20240110-105846_20240110-105907",
        "20240114-104610_20240114-104622",
        "20240118-145444_20240118-145503",
        "20240118-102918_20240118-102937",
        "20240122-202009_20240122-202023",
        "20240125-153036_20240125-153058",
        "20240122-193636_20240122-193650",
        "20240125-150434_20240125-150454",
        "20240113-140310_20240113-140325",
        "20240111-212547_20240111-212600",
        "20240124-135555_20240124-135609",
        "20240117-204305_20240117-204323",
        "20240111-135327_20240111-135343",
        "20240112-200742_20240112-200757",
        "20240123-112918_20240123-112931",
        "20240113-193110_20240113-193133",
        "20240120-210546_20240120-210558",
        "20240114-140036_20240114-140052",
        "20240111-111952_20240111-112009",
        "20240116-194230_20240116-194251",
        "20240109-111109_20240109-111124",
        "20240115-110458_20240115-110510",
        "20240120-191923_20240120-191935",
        "20240113-215005_20240113-215026",
        "20240120-131157_20240120-131212",
        "20240119-211029_20240119-211053",
        "20240122-204537_20240122-204601",
        "20240112-231728_20240112-231752",
        "20240110-202446_20240110-202459",
        "20240118-145353_20240118-145413",
        "20240112-142720_20240112-142742",
        "20240117-134346_20240117-134409",
        "20240111-225006_20240111-225021",
        "20240125-203624_20240125-203638",
        "20240116-142039_20240116-142104",
        "20240116-201244_20240116-201308",
        "20240117-105924_20240117-105945",
        "20240123-211203_20240123-211220",
        "20240125-121727_20240125-121738",
        "20240118-210835_20240118-210855",
        "20240123-193317_20240123-193336",
        "20240115-210935_20240115-210952",
        "20240119-205635_20240119-205651",
        "20240113-214224_20240113-214238",
        "20240120-204716_20240120-204727",
        "20240119-203032_20240119-203048",
        "20240110-202118_20240110-202139",
        "20240118-102609_20240118-102634",
        "20240118-200302_20240118-200318",
        "20240111-154417_20240111-154431",
        "20240109-134606_20240109-134618",
        "20240109-144349_20240109-144412",
        "20240114-221633_20240114-221649",
        "20240126-110213_20240126-110232",
        "20240105-152051_20240105-152108",
        "20240122-204035_20240122-204058",
        "20240123-201117_20240123-201130",
        "20240124-155253_20240124-155308",
        "20231219-110153_20231219-110218",
        "20240120-195009_20240120-195033",
        "20240119-193146_20240119-193200",
        "20240117-114533_20240117-114554",
        "20240111-142153_20240111-142212",
        "20240123-140523_20240123-140535",
        "20240111-202857_20240111-202919",
        "20240123-133312_20240123-133325",
        "20240126-112512_20240126-112533",
        "20240109-222744_20240109-222809",
        "20240112-201103_20240112-201128",
        "20240114-205737_20240114-205757",
        "20240124-215220_20240124-215237",
        "20240110-110128_20240110-110141",
        "20240113-200751_20240113-200802",
        "20240118-204340_20240118-204403",
        "20240113-140342_20240113-140354",
        "20240125-214019_20240125-214031",
        "20240120-201835_20240120-201851",
        "20240110-213656_20240110-213707",
        "20240113-140206_20240113-140232",
        "20240111-135401_20240111-135414",
        "20240116-205346_20240116-205405",
        "20240110-205835_20240110-205849",
        "20240113-155707_20240113-155732",
        "20240114-143528_20240114-143546",
        "20240125-202127_20240125-202153",
        "20240122-201404_20240122-201428",
        "20240115-213314_20240115-213328",
        "20240126-112101_20240126-112126",
        "20240119-202944_20240119-203003",
        "20240119-195745_20240119-195807",
        "20240119-210003_20240119-210018",
        "20240113-212243_20240113-212302",
        "20240110-222958_20240110-223013",
        "20240125-223715_20240125-223728",
        "20240111-212507_20240111-212528",
        "20240114-200954_20240114-201018",
        "20240119-211119_20240119-211132",
        "20240123-194339_20240123-194357",
        "20240113-214146_20240113-214201",
        "20240116-143728_20240116-143751",
        "20240116-205419_20240116-205430",
        "20240114-221312_20240114-221328",
        "20240118-201013_20240118-201038",
        "20240115-142803_20240115-142814",
        "20240112-204047_20240112-204102",
        "20240116-195959_20240116-200014",
        "20240112-204113_20240112-204131",
        "20240113-140833_20240113-140855",
        "20240115-202033_20240115-202052",
        "20240117-195356_20240117-195419",
        "20240119-111108_20240119-111129",
        "20240120-134328_20240120-134343",
        "20240119-211428_20240119-211447",
        "20240115-202911_20240115-202925",
        "20240117-193607_20240117-193633",
        "20240125-142028_20240125-142043",
        "20240119-195201_20240119-195215",
        "20240119-150525_20240119-150537",
        "20240120-192827_20240120-192841",
        "20240125-212535_20240125-212556",
        "20240125-150808_20240125-150826",
        "20240120-140913_20240120-140929",
        "20240119-111739_20240119-111749",
        "20240116-200904_20240116-200920",
        "20240116-135856_20240116-135921",
        "20240112-151158_20240112-151210",
        "20240116-195126_20240116-195147",
        "20240123-200654_20240123-200708",
        "20231128-161055_20231128-161110",
        "20240113-191313_20240113-191336",
        "20240123-140633_20240123-140645",
        "20240119-211122_20240119-211139",
        "20240111-204522_20240111-204541",
        "20240110-202219_20240110-202238",
        "20240125-202206_20240125-202217",
        "20240111-105613_20240111-105625",
        "20240126-112911_20240126-112930",
        "20240109-111006_20240109-111020",
        "20240110-105817_20240110-105837",
        "20240116-143630_20240116-143648",
        "20231130-191922_20231130-191936"
    ],
    "val": [
        "20240116-204917_20240116-204935",
        "20240116-192956_20240116-193019",
        "20240115-201327_20240115-201350",
        "20240111-135527_20240111-135538",
        "20240122-210433_20240122-210454",
        "20240125-200208_20240125-200227",
        "20240119-193447_20240119-193506",
        "20240119-153851_20240119-153902",
        "20240104-103013_20240104-103029",
        "20240120-194939_20240120-195000",
        "20240123-200232_20240123-200242",
        "20240112-231608_20240112-231625",
        "20240123-140823_20240123-140833",
        "20240120-140936_20240120-140953",
        "20240114-133923_20240114-133947",
        "20240113-214644_20240113-214709",
        "20240116-205955_20240116-210017",
        "20231128-161134_20231128-161150",
        "20240114-211758_20240114-211816",
        "20240125-212930_20240125-212943",
        "20240122-193807_20240122-193823",
        "20240116-112642_20240116-112701",
        "20240112-141206_20240112-141229",
        "20240116-205438_20240116-205449",
        "20240124-140914_20240124-140924",
        "20240109-145742_20240109-145803",
        "20240118-102505_20240118-102526",
        "20240118-133129_20240118-133149",
        "20240111-213301_20240111-213320",
        "20240115-202936_20240115-202953",
        "20240116-195928_20240116-195946",
        "20240114-143506_20240114-143519",
        "20240115-212646_20240115-212701",
        "20240122-131143_20240122-131203",
        "20240113-212037_20240113-212055",
        "20231130-191129_20231130-191143",
        "20240122-145026_20240122-145041",
        "20240110-220626_20240110-220637",
        "20240118-112758_20240118-112811",
        "20240110-195039_20240110-195055",
        "20240123-141300_20240123-141316",
        "20240119-202632_20240119-202650",
        "20240113-212336_20240113-212355",
        "20240307-200032_20240307-200048",
        "20231215-201015_20231215-201030",
        "20240119-221419_20240119-221436",
        "20240123-140311_20240123-140332",
        "20240113-214929_20240113-214949",
        "20240113-141603_20240113-141615",
        "20240126-111909_20240126-111921",
        "20240112-231653_20240112-231718",
        "20240120-115437_20240120-115452",
        "20240118-102252_20240118-102313",
        "20240119-212500_20240119-212515",
        "20240125-211651_20240125-211704",
        "20240112-201538_20240112-201552",
        "20240112-111355_20240112-111417",
        "20240109-134359_20240109-134416",
        "20240118-211026_20240118-211048",
        "20240124-140818_20240124-140831",
        "20240115-201555_20240115-201610",
        "20240119-212243_20240119-212259",
        "20240118-143930_20240118-143949",
        "20240104-104527_20240104-104548",
        "20240119-110957_20240119-111010",
        "20240113-140516_20240113-140534",
        "20240124-145044_20240124-145106",
        "20240117-192721_20240117-192737",
        "20240125-210303_20240125-210322",
        "20240126-110517_20240126-110539",
        "20240109-215000_20240109-215018",
        "20240109-215950_20240109-220000",
        "20240118-111154_20240118-111216",
        "20231128-161342_20231128-161358",
        "20240111-224246_20240111-224258",
        "20240120-113733_20240120-113745",
        "20240123-135901_20240123-135912",
        "20240111-200622_20240111-200635",
        "20240115-202935_20240115-202953",
        "20240113-142314_20240113-142330",
        "20240122-205824_20240122-205836",
        "20231128-161230_20231128-161240",
        "20240125-223353_20240125-223407",
        "20240124-144341_20240124-144359",
        "20240118-204512_20240118-204535",
        "20240119-143740_20240119-143751",
        "20240124-140302_20240124-140313",
        "20240113-205219_20240113-205230",
        "20240112-204208_20240112-204221",
        "20240122-112157_20240122-112222",
        "20240118-205019_20240118-205036",
        "20240114-104526_20240114-104538",
        "20240115-210117_20240115-210128",
        "20240119-141142_20240119-141152",
        "20240115-135858_20240115-135915",
        "20240122-200828_20240122-200845",
        "20240114-213523_20240114-213534",
        "20240122-145116_20240122-145134",
        "20240118-145615_20240118-145632",
        "20240119-142926_20240119-142941",
        "20240120-152045_20240120-152057",
        "20240123-201432_20240123-201450",
        "20240109-210118_20240109-210138",
        "20240111-150123_20240111-150135",
        "20240112-205910_20240112-205927",
        "20240112-204020_20240112-204034",
        "20240111-224200_20240111-224213",
        "20240110-144123_20240110-144136",
        "20240110-195136_20240110-195152",
        "20240116-111224_20240116-111245",
        "20240113-214425_20240113-214435",
        "20240119-153102_20240119-153124",
        "20240125-142219_20240125-142232",
        "20240118-145246_20240118-145308",
        "20240126-112940_20240126-112951",
        "20240118-103118_20240118-103137",
        "20240119-152906_20240119-152929",
        "20240113-135918_20240113-135931",
        "20240124-215832_20240124-215848",
        "20240113-200813_20240113-200824",
        "20240125-152729_20240125-152745",
        "20240122-210102_20240122-210112",
        "20240126-135327_20240126-135338"
    ]
}


BASE_TARGET_COLOR_ONLY_COPIES = 4
BASE_LONG_TAIL_COLOR_ONLY_COPIES = 6
BASE_TARGET_AND_LONG_TAIL_COLOR_ONLY_COPIES = 8
TARGET_CLASS_COLOR_PRIORITY = 0.75
LONG_TAIL_CLASS_COLOR_PRIORITY = 1.5
PEDESTRIAN_COLOR_PRIORITY_BONUS = 0.75
TARGET_AND_LONG_TAIL_PRIORITY_BONUS = 2.0
VERY_LIGHT_PHOTO_CLASSES = frozenset({"bus", "animal", "construction_vehicle"})
LIGHT_PHOTO_CLASSES = frozenset({"pedestrian", "bicycle", "motor", "traffic_cone"})
COLOR_INTENSITY_CYCLES: Dict[str, Tuple[str, ...]] = {
    "target_tail": ("light", "medium", "strong", "medium"),
    "long_tail": ("medium", "strong", "xstrong", "strong"),
    "target_and_long_tail": ("medium", "strong", "xstrong", "strong", "medium"),
}
PHOTO_INTENSITY_SCALES: Dict[str, Dict[str, float]] = {
    "light": {"magnitude_scale": 0.85, "probability_scale": 0.90},
    "medium": {"magnitude_scale": 1.0, "probability_scale": 1.0},
    "strong": {"magnitude_scale": 1.35, "probability_scale": 1.15},
    "xstrong": {"magnitude_scale": 1.70, "probability_scale": 1.30},
}

PHOTO_PROFILES: Dict[str, Dict[str, Any]] = {
    "very_light": {
        "brightness_px": 8.0,
        "contrast_delta": 0.06,
        "gamma_delta": 0.05,
        "gamma_prob": 0.45,
        "wb_delta": 0.04,
        "wb_prob": 0.40,
        "saturation_delta": 0.06,
        "saturation_prob": 0.35,
        "shadow_prob": 0.15,
        "shadow_strength": (0.88, 0.96),
        "fog_prob": 0.10,
        "fog_strength": (0.03, 0.08),
        "blur_prob": 0.12,
        "blur_kernels": (3,),
        "noise_prob": 0.10,
        "noise_sigma": (1.0, 3.0),
        "jpeg_prob": 0.25,
        "jpeg_quality": (90, 98),
    },
    "light": {
        "brightness_px": 12.0,
        "contrast_delta": 0.10,
        "gamma_delta": 0.08,
        "gamma_prob": 0.55,
        "wb_delta": 0.06,
        "wb_prob": 0.45,
        "saturation_delta": 0.08,
        "saturation_prob": 0.40,
        "shadow_prob": 0.20,
        "shadow_strength": (0.84, 0.94),
        "fog_prob": 0.12,
        "fog_strength": (0.04, 0.10),
        "blur_prob": 0.16,
        "blur_kernels": (3,),
        "noise_prob": 0.15,
        "noise_sigma": (1.0, 4.0),
        "jpeg_prob": 0.30,
        "jpeg_quality": (88, 96),
    },
    "medium": {
        "brightness_px": 18.0,
        "contrast_delta": 0.15,
        "gamma_delta": 0.12,
        "gamma_prob": 0.60,
        "wb_delta": 0.08,
        "wb_prob": 0.50,
        "saturation_delta": 0.10,
        "saturation_prob": 0.45,
        "shadow_prob": 0.25,
        "shadow_strength": (0.78, 0.92),
        "fog_prob": 0.15,
        "fog_strength": (0.05, 0.12),
        "blur_prob": 0.20,
        "blur_kernels": (3, 5),
        "noise_prob": 0.18,
        "noise_sigma": (1.0, 5.0),
        "jpeg_prob": 0.35,
        "jpeg_quality": (85, 95),
    },
}

CANONICAL_CLASS_MAP: Dict[str, str] = {
    "car": "car",
    "truck": "truck",
    "bus": "bus",
    "construction_vehicle": "construction_vehicle",
    "pedestrian": "pedestrian",
    "motor": "motor",
    "bicycle": "bicycle",
    "animal": "animal",
    "traffic_cone": "traffic_cone",
    "barrier": "barrier",
    "stopper": "stopper",
    "trash_bin": "trash_bin",
    "sign": "sign",
    "普通轿车": "car",
    "敞篷车": "car",
    "敞篷轿车": "car",
    "SUV": "car",
    "MPV": "car",
    "面包车": "car",
    "皮卡": "car",
    "警车": "car",
    "救护车": "car",
    "其他机动车": "car",
    "未知机动车": "car",
    "未知机动车车轮": "car",
    "未知机动车车灯": "car",
    "房车": "truck",
    "普通小型货车": "truck",
    "箱式小型货车": "truck",
    "普通大型货车": "truck",
    "轻运货车": "truck",
    "轿运车": "truck",
    "客车": "bus",
    "校车": "bus",
    "普通公交车": "bus",
    "铰链公交车": "bus",
    "有轨电车": "bus",
    "无轨电车": "bus",
    "消防车": "construction_vehicle",
    "清扫车": "construction_vehicle",
    "清洁车": "construction_vehicle",
    "工程车": "construction_vehicle",
    "拖车": "construction_vehicle",
    "拖拉机": "construction_vehicle",
    "叉车": "construction_vehicle",
    "油罐车": "construction_vehicle",
    "成人": "pedestrian",
    "儿童": "pedestrian",
    "交警": "pedestrian",
    "环卫工人": "pedestrian",
    "道路施工人员": "pedestrian",
    "两轮摩托": "motor",
    "三轮摩托": "motor",
    "两轮电动车": "bicycle",
    "三轮电动车": "bicycle",
    "两轮自行车": "bicycle",
    "三轮自行车": "bicycle",
    "滑板车": "bicycle",
    "平衡车": "bicycle",
    "婴儿车": "bicycle",
    "轮椅": "bicycle",
    "平板小推车": "bicycle",
    "超市购物车": "bicycle",
    "手推车": "bicycle",
    "其他非机动车": "bicycle",
    "未知非机动车": "bicycle",
    "非机动车组": "bicycle",
    "小型动物": "animal",
    "大型动物": "animal",
    "锥桶": "traffic_cone",
    "锥筒": "traffic_cone",
    "防撞桶": "barrier",
    "路障": "barrier",
    "路桩": "barrier",
    "石墩": "barrier",
    "水马": "barrier",
    "柱子": "barrier",
    "石块": "barrier",
    "树枝/树杈": "barrier",
    "树枝/树桩": "barrier",
    "空中漂浮物": "barrier",
    "路坑/水洼": "barrier",
    "路坑": "barrier",
    "其他静态障碍物": "barrier",
    "车位停止器": "stopper",
    "车位止停器": "stopper",
    "车位锁": "stopper",
    "减速带": "stopper",
    "垃圾桶": "trash_bin",
    "灭火器": "trash_bin",
    "箱子": "trash_bin",
    "A字牌": "sign",
    "三角牌": "sign",
    "施工警示牌": "sign",
    "道闸杆": "sign",
    "construcion_sign": "sign",
}


@dataclass(frozen=True)
class FrameDecision:
    trigger: str
    profile: str
    canonical_classes: Tuple[str, ...]
    class_counts: Dict[str, int]
    has_target_tail: bool
    has_long_tail: bool
    target_tail_count: int
    long_tail_count: int
    base_color_only_count: int
    color_priority: float


def wrap_to_pi(angle: float) -> float:
    return (angle + math.pi) % (2.0 * math.pi) - math.pi


def canonicalize_class(class_name: str) -> str:
    key = str(class_name).strip()
    return CANONICAL_CLASS_MAP.get(key, key if key.isascii() else "unknown")


def load_json(path: Path) -> Any:
    with path.open("r", encoding="utf-8") as handle:
        return json.load(handle)


def save_json(data: Any, path: Path) -> None:
    with path.open("w", encoding="utf-8") as handle:
        json.dump(data, handle, ensure_ascii=False, indent=2)


def stable_rng(seed: int, *parts: str) -> np.random.Generator:
    digest = hashlib.sha1("|".join([str(seed), *parts]).encode("utf-8")).digest()
    child_seed = int.from_bytes(digest[:8], "little", signed=False)
    return np.random.default_rng(child_seed)


def _collect_scene_frame_decisions_worker(
    args: Tuple[Path, Optional[int], bool]
) -> Tuple[Path, List[str], Dict[str, FrameDecision]]:
    scene_dir, max_frames, augment_enabled = args
    frame_ids, frame_decisions = collect_scene_frame_decisions(
        scene_dir, max_frames=max_frames, augment_enabled=augment_enabled
    )
    return scene_dir, frame_ids, frame_decisions


def _run_single_scene_worker(
    args: Tuple[Path, Path, int, bool, Optional[int], bool, Mapping[str, int], bool, str]
) -> Dict[str, Any]:
    return run_single_scene(*args)


def ensure_output_root(
    scene_dir: Path, output_dir: Path, copy_originals: bool, overwrite_output: bool
) -> None:
    if output_dir.exists():
        if not overwrite_output:
            raise FileExistsError(
                f"Output directory already exists: {output_dir}. "
                "Please remove it, pass --overwrite-output, or choose another --output-dir."
            )
        shutil.rmtree(output_dir)
    if copy_originals:
        try:
            output_dir.relative_to(scene_dir)
        except ValueError:
            return
        raise ValueError(
            "When --copy-originals is enabled, --output-dir must not be inside --scene-dir."
        )


def find_single_file(directory: Path, stem: str) -> Optional[Path]:
    matches = [path for path in directory.glob(f"{stem}.*") if path.is_file()]
    if len(matches) == 1:
        return matches[0]
    if len(matches) > 1:
        preferred_suffixes = [".jpeg", ".jpg", ".png", ".json"]
        for suffix in preferred_suffixes:
            for path in matches:
                if path.suffix.lower() == suffix:
                    return path
        return sorted(matches)[0]
    return None


def collect_frame_ids(scene_dir: Path) -> List[str]:
    result_dir = scene_dir / "result"
    frame_ids = sorted(path.stem for path in result_dir.glob("*.json"))
    valid_ids: List[str] = []
    for frame_id in frame_ids:
        required = [
            scene_dir / "result" / f"{frame_id}.json",
            scene_dir / "camera_config" / f"{frame_id}.json",
            scene_dir / "position" / f"{frame_id}.json",
        ]
        if not all(path.exists() for path in required):
            continue
        image_paths = [find_single_file(scene_dir / image_dir, frame_id) for image_dir in IMAGE_DIRS]
        if any(path is None for path in image_paths):
            continue
        valid_ids.append(frame_id)
    return valid_ids


def collect_object_counts(label_data: Mapping[str, Any]) -> Counter[str]:
    counts: Counter[str] = Counter()
    for obj in label_data.get("objects", []):
        if obj.get("type", "3D_BOX") != "3D_BOX":
            continue
        class_name = canonicalize_class(obj.get("className", ""))
        if class_name == "unknown":
            continue
        counts[class_name] += 1
    return counts


def compute_base_color_only_count(has_target_tail: bool, has_long_tail: bool) -> int:
    if has_target_tail and has_long_tail:
        return BASE_TARGET_AND_LONG_TAIL_COLOR_ONLY_COPIES
    if has_long_tail:
        return BASE_LONG_TAIL_COLOR_ONLY_COPIES
    if has_target_tail:
        return BASE_TARGET_COLOR_ONLY_COPIES
    return 0


def compute_color_priority(
    class_counts: Mapping[str, int], target_tail_count: int, long_tail_count: int
) -> float:
    has_target_tail = target_tail_count > 0
    has_long_tail = long_tail_count > 0
    priority = float(compute_base_color_only_count(has_target_tail, has_long_tail))
    priority += float(target_tail_count) * TARGET_CLASS_COLOR_PRIORITY
    priority += float(long_tail_count) * LONG_TAIL_CLASS_COLOR_PRIORITY
    if class_counts.get("pedestrian", 0) > 0:
        priority += PEDESTRIAN_COLOR_PRIORITY_BONUS
    if has_target_tail and has_long_tail:
        priority += TARGET_AND_LONG_TAIL_PRIORITY_BONUS
    return priority


def decide_frame_augmentation(label_data: Mapping[str, Any]) -> Optional[FrameDecision]:
    class_counts = collect_object_counts(label_data)
    if not class_counts:
        return None

    canonical_classes = sorted(class_counts)
    class_set = set(canonical_classes)
    target_tail_count = sum(class_counts.get(name, 0) for name in TARGET_TAIL_CLASSES)
    long_tail_count = sum(class_counts.get(name, 0) for name in LONG_TAIL_CLASSES)
    has_target_tail = target_tail_count > 0
    has_long_tail = long_tail_count > 0

    if not has_target_tail and not has_long_tail:
        return None

    if has_target_tail and has_long_tail:
        trigger = "target_and_long_tail"
    elif has_long_tail:
        trigger = "long_tail"
    else:
        trigger = "target_tail"

    if class_set & VERY_LIGHT_PHOTO_CLASSES:
        profile = "very_light"
    elif class_set & LIGHT_PHOTO_CLASSES:
        profile = "light"
    else:
        profile = "medium"

    return FrameDecision(
        trigger=trigger,
        profile=profile,
        canonical_classes=tuple(canonical_classes),
        class_counts=dict(sorted(class_counts.items())),
        has_target_tail=has_target_tail,
        has_long_tail=has_long_tail,
        target_tail_count=target_tail_count,
        long_tail_count=long_tail_count,
        base_color_only_count=compute_base_color_only_count(has_target_tail, has_long_tail),
        color_priority=round(
            compute_color_priority(class_counts, target_tail_count, long_tail_count), 3
        ),
    )


def horizontal_flip_bbox(box: Sequence[float], width: int) -> List[float]:
    x1, y1, x2, y2 = [float(value) for value in box]
    new_x1 = float(width - 1 - x2)
    new_x2 = float(width - 1 - x1)
    x_min, x_max = sorted((new_x1, new_x2))
    x_min = min(max(x_min, 0.0), float(width - 1))
    x_max = min(max(x_max, 0.0), float(width - 1))
    return [x_min, y1, x_max, y2]


def augment_label_json(
    label_data: Mapping[str, Any], image_widths: Mapping[str, int], do_flip: bool
) -> Dict[str, Any]:
    augmented = copy.deepcopy(label_data)
    if not do_flip:
        return augmented

    for obj in augmented.get("objects", []):
        contour = obj.get("contour", {})
        center3d = contour.get("center3D")
        if isinstance(center3d, MutableMapping) and "y" in center3d:
            center3d["y"] = -float(center3d["y"])

        rotation3d = contour.get("rotation3D")
        if isinstance(rotation3d, MutableMapping) and "z" in rotation3d:
            rotation3d["z"] = wrap_to_pi(-float(rotation3d["z"]))

        bbox_2d = obj.get("2D_bbox")
        if isinstance(bbox_2d, MutableMapping):
            for image_key, box in list(bbox_2d.items()):
                width = image_widths.get(image_key)
                if width is None or not isinstance(box, Sequence) or len(box) != 4:
                    continue
                bbox_2d[image_key] = horizontal_flip_bbox(box, width)

    return augmented


def decode_camera_external(camera_cfg: Mapping[str, Any]) -> np.ndarray:
    matrix = np.asarray(camera_cfg["camera_external"], dtype=np.float64).reshape(4, 4)
    row_major = str(camera_cfg.get("rowMajor", "true")).lower() == "true"
    return matrix if row_major else matrix.T


def encode_camera_external(matrix: np.ndarray, row_major_flag: Any) -> List[float]:
    row_major = str(row_major_flag).lower() == "true"
    serial = matrix if row_major else matrix.T
    return [float(value) for value in serial.reshape(-1).tolist()]


def augment_camera_config(
    camera_config: Sequence[Mapping[str, Any]], do_flip: bool
) -> List[Dict[str, Any]]:
    augmented = copy.deepcopy(list(camera_config))
    if not do_flip:
        return augmented

    for camera_cfg in augmented:
        width = int(camera_cfg["width"])
        camera_internal = camera_cfg.get("camera_internal", {})
        if "cx" in camera_internal:
            camera_internal["cx"] = float(width - 1 - float(camera_internal["cx"]))

        matrix = decode_camera_external(camera_cfg)
        mirrored_matrix = CAMERA_X_MIRROR @ matrix @ LIDAR_MIRROR
        camera_cfg["camera_external"] = encode_camera_external(
            mirrored_matrix, camera_cfg.get("rowMajor", "false")
        )

    return augmented


def augment_position_json(position_data: Mapping[str, Any], new_frame_id: str) -> Dict[str, Any]:
    augmented = copy.deepcopy(position_data)
    augmented["name"] = new_frame_id
    return augmented


def make_identity_photometric_plan(profile_name: str) -> Dict[str, Any]:
    return {
        "profile": profile_name,
        "intensity": "identity",
        "brightness_shift": 0.0,
        "contrast_scale": 1.0,
        "gamma": None,
        "wb_gains_bgr": None,
        "saturation_scale": None,
        "shadow_strength": None,
        "fog_strength": None,
        "blur_kernel": 0,
        "noise_sigma": 0.0,
        "jpeg_quality": 100,
    }


def scale_probability(probability: float, scale: float) -> float:
    return min(max(probability * scale, 0.0), 1.0)


def scale_range_from_neutral(
    values: Sequence[float], neutral: float, scale: float
) -> Tuple[float, float]:
    scaled_values = [neutral + (float(value) - neutral) * scale for value in values]
    low, high = sorted(scaled_values)
    return low, high


def sample_photometric_plan(
    rng: np.random.Generator, profile_name: str, intensity_name: str
) -> Dict[str, Any]:
    if intensity_name == "identity":
        return make_identity_photometric_plan(profile_name)

    cfg = PHOTO_PROFILES[profile_name]
    intensity_cfg = PHOTO_INTENSITY_SCALES[intensity_name]
    magnitude_scale = float(intensity_cfg["magnitude_scale"])
    probability_scale = float(intensity_cfg["probability_scale"])

    brightness_shift = float(
        rng.uniform(
            -cfg["brightness_px"] * magnitude_scale,
            cfg["brightness_px"] * magnitude_scale,
        )
    )
    contrast_delta = float(cfg["contrast_delta"]) * magnitude_scale
    contrast_scale = float(rng.uniform(1.0 - contrast_delta, 1.0 + contrast_delta))

    gamma_value: Optional[float] = None
    gamma_delta = float(cfg["gamma_delta"]) * magnitude_scale
    if rng.random() < scale_probability(float(cfg["gamma_prob"]), probability_scale):
        gamma_value = float(rng.uniform(1.0 - gamma_delta, 1.0 + gamma_delta))

    wb_gains: Optional[List[float]] = None
    wb_delta = float(cfg["wb_delta"]) * magnitude_scale
    if rng.random() < scale_probability(float(cfg["wb_prob"]), probability_scale):
        wb_gains = [
            float(rng.uniform(1.0 - wb_delta, 1.0 + wb_delta))
            for _ in range(3)
        ]

    saturation_scale: Optional[float] = None
    saturation_delta = float(cfg["saturation_delta"]) * magnitude_scale
    if rng.random() < scale_probability(float(cfg["saturation_prob"]), probability_scale):
        saturation_scale = float(
            rng.uniform(1.0 - saturation_delta, 1.0 + saturation_delta)
        )

    shadow_strength: Optional[float] = None
    if rng.random() < scale_probability(float(cfg["shadow_prob"]), probability_scale):
        shadow_range = scale_range_from_neutral(cfg["shadow_strength"], neutral=1.0, scale=magnitude_scale)
        shadow_strength = float(rng.uniform(*shadow_range))

    fog_strength: Optional[float] = None
    if rng.random() < scale_probability(float(cfg["fog_prob"]), probability_scale):
        fog_low, fog_high = [float(value) * magnitude_scale for value in cfg["fog_strength"]]
        fog_strength = float(rng.uniform(fog_low, fog_high))

    blur_kernel = 0
    if rng.random() < scale_probability(float(cfg["blur_prob"]), probability_scale):
        blur_kernels = sorted({int(value) for value in cfg["blur_kernels"]})
        if magnitude_scale >= 1.35:
            blur_kernels.append(5)
        if magnitude_scale >= 1.60:
            blur_kernels.append(7)
        blur_kernel = int(rng.choice(sorted(set(blur_kernels))))

    noise_sigma = 0.0
    if rng.random() < scale_probability(float(cfg["noise_prob"]), probability_scale):
        noise_low, noise_high = [float(value) * magnitude_scale for value in cfg["noise_sigma"]]
        noise_sigma = float(rng.uniform(noise_low, noise_high))

    jpeg_quality = 100
    if rng.random() < scale_probability(float(cfg["jpeg_prob"]), probability_scale):
        base_quality = int(rng.integers(cfg["jpeg_quality"][0], cfg["jpeg_quality"][1] + 1))
        jpeg_quality = int(np.clip(round(100.0 - (100.0 - base_quality) * magnitude_scale), 1, 100))

    return {
        "profile": profile_name,
        "intensity": intensity_name,
        "brightness_shift": brightness_shift,
        "contrast_scale": contrast_scale,
        "gamma": gamma_value,
        "wb_gains_bgr": wb_gains,
        "saturation_scale": saturation_scale,
        "shadow_strength": shadow_strength,
        "fog_strength": fog_strength,
        "blur_kernel": blur_kernel,
        "noise_sigma": noise_sigma,
        "jpeg_quality": jpeg_quality,
    }


def adjust_brightness_contrast(
    image: np.ndarray, contrast_scale: float, brightness_shift: float
) -> np.ndarray:
    out = image.astype(np.float32) * contrast_scale + brightness_shift
    return np.clip(out, 0, 255).astype(np.uint8)


def adjust_gamma(image: np.ndarray, gamma: float) -> np.ndarray:
    gamma = max(gamma, 1e-6)
    inverse_gamma = 1.0 / gamma
    lut = np.array(
        [((index / 255.0) ** inverse_gamma) * 255.0 for index in range(256)],
        dtype=np.uint8,
    )
    return cv2.LUT(image, lut)


def adjust_white_balance(image: np.ndarray, gains_bgr: Sequence[float]) -> np.ndarray:
    gains = np.asarray(gains_bgr, dtype=np.float32).reshape(1, 1, 3)
    out = image.astype(np.float32) * gains
    return np.clip(out, 0, 255).astype(np.uint8)


def adjust_saturation(image: np.ndarray, scale: float) -> np.ndarray:
    hsv = cv2.cvtColor(image, cv2.COLOR_BGR2HSV).astype(np.float32)
    hsv[:, :, 1] *= scale
    hsv[:, :, 1] = np.clip(hsv[:, :, 1], 0, 255)
    return cv2.cvtColor(hsv.astype(np.uint8), cv2.COLOR_HSV2BGR)


def apply_shadow(image: np.ndarray, rng: np.random.Generator, strength: float) -> np.ndarray:
    height, width = image.shape[:2]
    polygon = np.array(
        [
            [0, int(rng.integers(0, max(1, height // 2)))],
            [width - 1, int(rng.integers(0, max(1, height // 2)))],
            [width - 1, int(rng.integers(max(1, height // 2), height))],
            [0, int(rng.integers(max(1, height // 2), height))],
        ],
        dtype=np.int32,
    )
    mask = np.zeros((height, width), dtype=np.float32)
    cv2.fillConvexPoly(mask, polygon, 1.0)
    mask = cv2.GaussianBlur(mask, (0, 0), sigmaX=max(width, height) / 12.0)
    shadow_gain = 1.0 - mask[..., None] * (1.0 - strength)
    out = image.astype(np.float32) * shadow_gain
    return np.clip(out, 0, 255).astype(np.uint8)


def apply_fog(image: np.ndarray, rng: np.random.Generator, strength: float) -> np.ndarray:
    height, width = image.shape[:2]
    center_x = float(rng.uniform(0, width))
    center_y = float(rng.uniform(0, height))
    radius = float(rng.uniform(max(width, height) * 0.55, max(width, height) * 1.1))
    yy, xx = np.mgrid[0:height, 0:width]
    dist = np.sqrt((xx - center_x) ** 2 + (yy - center_y) ** 2).astype(np.float32)
    fog_mask = np.exp(-(dist ** 2) / (2.0 * (radius ** 2)))
    fog_mask = (fog_mask / max(float(fog_mask.max()), 1e-6)) * strength
    fog_mask = fog_mask[..., None]
    white = np.full_like(image, 255, dtype=np.float32)
    out = image.astype(np.float32) * (1.0 - fog_mask) + white * fog_mask
    return np.clip(out, 0, 255).astype(np.uint8)


def add_gaussian_noise(image: np.ndarray, rng: np.random.Generator, sigma: float) -> np.ndarray:
    noise = rng.normal(0.0, sigma, size=image.shape).astype(np.float32)
    out = image.astype(np.float32) + noise
    return np.clip(out, 0, 255).astype(np.uint8)


def simulate_jpeg(image: np.ndarray, quality: int) -> np.ndarray:
    quality = int(np.clip(quality, 1, 100))
    success, encoded = cv2.imencode(".jpg", image, [int(cv2.IMWRITE_JPEG_QUALITY), quality])
    if not success:
        return image
    decoded = cv2.imdecode(encoded, cv2.IMREAD_COLOR)
    return image if decoded is None else decoded


def apply_photometric_plan(
    image: np.ndarray, plan: Mapping[str, Any], image_rng: np.random.Generator
) -> np.ndarray:
    out = adjust_brightness_contrast(
        image, float(plan["contrast_scale"]), float(plan["brightness_shift"])
    )
    if plan.get("gamma") is not None:
        out = adjust_gamma(out, float(plan["gamma"]))
    if plan.get("wb_gains_bgr") is not None:
        out = adjust_white_balance(out, plan["wb_gains_bgr"])
    if plan.get("saturation_scale") is not None:
        out = adjust_saturation(out, float(plan["saturation_scale"]))
    if plan.get("shadow_strength") is not None:
        out = apply_shadow(out, image_rng, float(plan["shadow_strength"]))
    if plan.get("fog_strength") is not None:
        out = apply_fog(out, image_rng, float(plan["fog_strength"]))
    blur_kernel = int(plan.get("blur_kernel", 0))
    if blur_kernel > 1:
        out = cv2.GaussianBlur(out, (blur_kernel, blur_kernel), sigmaX=0.0)
    noise_sigma = float(plan.get("noise_sigma", 0.0))
    if noise_sigma > 0:
        out = add_gaussian_noise(out, image_rng, noise_sigma)
    jpeg_quality = int(plan.get("jpeg_quality", 100))
    if jpeg_quality < 100:
        out = simulate_jpeg(out, jpeg_quality)
    return out


def choose_color_intensity(decision: FrameDecision, color_only_index: int) -> str:
    cycle = COLOR_INTENSITY_CYCLES[decision.trigger]
    return cycle[color_only_index % len(cycle)]


def load_frame_images(scene_dir: Path, frame_id: str) -> Dict[str, Tuple[np.ndarray, str]]:
    source_images: Dict[str, Tuple[np.ndarray, str]] = {}
    for image_dir in IMAGE_DIRS:
        src_path = find_single_file(scene_dir / image_dir, frame_id)
        if src_path is None:
            raise FileNotFoundError(f"Missing {image_dir} image for frame {frame_id}")
        image = cv2.imread(str(src_path), cv2.IMREAD_COLOR)
        if image is None:
            raise RuntimeError(f"Failed to read image: {src_path}")
        source_images[image_dir] = (image, src_path.suffix.lower())
    return source_images


def load_frame_metadata(
    scene_dir: Path, frame_id: str
) -> Tuple[Mapping[str, Any], Sequence[Mapping[str, Any]], Mapping[str, Any]]:
    return (
        load_json(scene_dir / "result" / f"{frame_id}.json"),
        load_json(scene_dir / "camera_config" / f"{frame_id}.json"),
        load_json(scene_dir / "position" / f"{frame_id}.json"),
    )


def save_augmented_images(
    source_images: Mapping[str, Tuple[np.ndarray, str]],
    output_dir: Path,
    new_frame_id: str,
    do_flip: bool,
    photometric_plan: Mapping[str, Any],
    seed: int,
) -> None:
    for image_dir, (source_image, suffix) in source_images.items():
        image = source_image.copy()
        if do_flip:
            image = cv2.flip(image, 1)

        image_rng = stable_rng(seed, new_frame_id, image_dir)
        image = apply_photometric_plan(image, photometric_plan, image_rng)

        dst_path = output_dir / image_dir / f"{new_frame_id}{suffix}"
        dst_path.parent.mkdir(parents=True, exist_ok=True)
        if not cv2.imwrite(str(dst_path), image):
            raise RuntimeError(f"Failed to write image: {dst_path}")


def build_image_widths(source_images: Mapping[str, Tuple[np.ndarray, str]]) -> Dict[str, int]:
    return {
        image_dir: int(image.shape[1])
        for image_dir, (image, _) in source_images.items()
    }


def save_augmented_metadata_files(
    output_dir: Path,
    new_frame_id: str,
    label_data: Mapping[str, Any],
    camera_config: Sequence[Mapping[str, Any]],
    position_data: Mapping[str, Any],
    do_flip: bool,
    image_widths: Mapping[str, int],
) -> None:
    save_json(
        augment_label_json(label_data, image_widths=image_widths, do_flip=do_flip),
        output_dir / "result" / f"{new_frame_id}.json",
    )
    save_json(
        augment_camera_config(camera_config, do_flip=do_flip),
        output_dir / "camera_config" / f"{new_frame_id}.json",
    )
    save_json(
        augment_position_json(position_data, new_frame_id=new_frame_id),
        output_dir / "position" / f"{new_frame_id}.json",
    )


def allocate_numeric_augmented_ids(
    frame_id: str, count: int, used_ids: set[int]
) -> List[str]:
    if not frame_id.isdigit():
        return [f"{frame_id}_{index + 1}" for index in range(count)]

    next_id = int(frame_id)
    new_ids: List[str] = []
    while len(new_ids) < count:
        next_id += 1
        if next_id in used_ids:
            continue
        used_ids.add(next_id)
        new_ids.append(str(next_id))
    return new_ids


def create_empty_scene_layout(output_dir: Path) -> None:
    for folder in (*METADATA_DIRS, *IMAGE_DIRS):
        (output_dir / folder).mkdir(parents=True, exist_ok=True)


def copy_scene_without_point_cloud(scene_dir: Path, output_dir: Path) -> None:
    create_empty_scene_layout(output_dir)
    for folder in (*METADATA_DIRS, *IMAGE_DIRS):
        src_dir = scene_dir / folder
        dst_dir = output_dir / folder
        if not src_dir.exists():
            continue
        for src_path in src_dir.iterdir():
            if src_path.is_file():
                shutil.copy2(src_path, dst_dir / src_path.name)


def build_output_dir(
    scene_dir: Path, output_dir: Path, copy_originals: bool, overwrite_output: bool
) -> None:
    ensure_output_root(scene_dir, output_dir, copy_originals, overwrite_output)
    if copy_originals:
        copy_scene_without_point_cloud(scene_dir, output_dir)
    else:
        create_empty_scene_layout(output_dir)


def maybe_limit_frame_ids(frame_ids: List[str], max_frames: Optional[int]) -> List[str]:
    if max_frames is None:
        return frame_ids
    return frame_ids[: max(0, max_frames)]


def collect_scene_frame_decisions(
    scene_dir: Path, max_frames: Optional[int], augment_enabled: bool = True
) -> Tuple[List[str], Dict[str, FrameDecision]]:
    frame_ids = maybe_limit_frame_ids(collect_frame_ids(scene_dir), max_frames=max_frames)
    if not augment_enabled:
        return frame_ids, {}

    frame_decisions: Dict[str, FrameDecision] = {}
    for frame_id in frame_ids:
        label_data = load_json(scene_dir / "result" / f"{frame_id}.json")
        decision = decide_frame_augmentation(label_data)
        if decision is not None:
            frame_decisions[frame_id] = decision
    return frame_ids, frame_decisions


def proportional_allocation(weights: Sequence[float], total_count: int) -> List[int]:
    if not weights:
        return []
    if total_count <= 0:
        return [0 for _ in weights]

    safe_weights = [max(float(weight), 0.0) for weight in weights]
    weight_sum = sum(safe_weights)
    if weight_sum <= 0.0:
        base = total_count // len(safe_weights)
        counts = [base for _ in safe_weights]
        for index in range(total_count - base * len(safe_weights)):
            counts[index] += 1
        return counts

    raw_counts = [total_count * weight / weight_sum for weight in safe_weights]
    counts = [int(math.floor(value)) for value in raw_counts]
    remaining = total_count - sum(counts)
    if remaining <= 0:
        return counts

    order = sorted(
        range(len(raw_counts)),
        key=lambda index: (-(raw_counts[index] - counts[index]), -safe_weights[index], index),
    )
    for index in order[:remaining]:
        counts[index] += 1
    return counts


def plan_scene_color_only_counts(
    scene_decisions: Mapping[str, Dict[str, FrameDecision]],
    target_color_only_frames: int,
) -> Dict[str, Dict[str, int]]:
    frame_keys: List[Tuple[str, str]] = []
    decisions: List[FrameDecision] = []
    for scene_name in sorted(scene_decisions):
        for frame_id in sorted(scene_decisions[scene_name]):
            frame_keys.append((scene_name, frame_id))
            decisions.append(scene_decisions[scene_name][frame_id])

    if not frame_keys:
        return {}

    required_color_only_frames = max(0, int(target_color_only_frames))
    base_color_only_counts = [decision.base_color_only_count for decision in decisions]

    if required_color_only_frames >= sum(base_color_only_counts):
        color_only_counts = list(base_color_only_counts)
        remaining_color_only_frames = required_color_only_frames - sum(color_only_counts)
        extra_color_only_counts = proportional_allocation(
            [decision.color_priority for decision in decisions], remaining_color_only_frames
        )
        color_only_counts = [
            base_count + extra_count
            for base_count, extra_count in zip(color_only_counts, extra_color_only_counts)
        ]
    else:
        color_only_counts = proportional_allocation(
            base_color_only_counts, required_color_only_frames
        )

    scene_color_only_counts: Dict[str, Dict[str, int]] = {}
    for (scene_name, frame_id), color_only_count in zip(frame_keys, color_only_counts):
        scene_color_only_counts.setdefault(scene_name, {})[frame_id] = color_only_count
    return scene_color_only_counts


def is_scene_dir(path: Path) -> bool:
    required_dirs = (*METADATA_DIRS, *IMAGE_DIRS)
    return path.is_dir() and all((path / folder).is_dir() for folder in required_dirs)


def collect_scene_dirs(scene_root: Path) -> List[Path]:
    if is_scene_dir(scene_root):
        return [scene_root]
    if not scene_root.is_dir():
        raise FileNotFoundError(f"Scene root does not exist: {scene_root}")
    scene_dirs = [path for path in scene_root.iterdir() if is_scene_dir(path)]
    return sorted(scene_dirs, key=lambda path: path.name)


def partition_scene_dirs_by_split(
    scene_root: Path,
    scene_dirs: Sequence[Path],
    split_scenes: Mapping[str, Sequence[str]],
) -> Dict[str, List[Path]]:
    scene_dir_by_name = {scene_dir.name: scene_dir for scene_dir in scene_dirs}
    all_split_scene_names = set(split_scenes["train"]) | set(split_scenes["val"])
    single_scene_input = is_scene_dir(scene_root)
    subset_scene_input = single_scene_input or len(scene_dir_by_name) < len(all_split_scene_names)

    missing_scene_names = {
        split_name: [
            scene_name
            for scene_name in split_scenes[split_name]
            if scene_name not in scene_dir_by_name
        ]
        for split_name in ("train", "val")
    }
    if any(missing_scene_names.values()):
        missing_details = ", ".join(
            f"{split_name}={len(scene_names)}"
            for split_name, scene_names in missing_scene_names.items()
            if scene_names
        )
        if subset_scene_input:
            print(
                "Info: current scene_root is a subset of DEFAULT_TRAIN_VAL_SPLIT; "
                f"ignoring scenes not present under {scene_root}: {missing_details}"
            )
        else:
            raise FileNotFoundError(
                "Some scenes declared in DEFAULT_TRAIN_VAL_SPLIT were not found under "
                f"{scene_root}: {missing_details}"
            )

    extra_scene_names = sorted(
        scene_name for scene_name in scene_dir_by_name if scene_name not in all_split_scene_names
    )
    if extra_scene_names:
        print(
            "Warning: skipping scenes not listed in DEFAULT_TRAIN_VAL_SPLIT: "
            + ", ".join(extra_scene_names)
        )

    return {
        split_name: [
            scene_dir_by_name[scene_name]
            for scene_name in split_scenes[split_name]
            if scene_name in scene_dir_by_name
        ]
        for split_name in ("train", "val")
    }


def run_single_scene(
    scene_dir: Path,
    output_dir: Path,
    seed: int,
    copy_originals: bool,
    max_frames: Optional[int],
    overwrite_output: bool,
    color_only_counts: Mapping[str, int],
    augment_enabled: bool,
    scene_split: str,
) -> Dict[str, Any]:
    build_output_dir(
        scene_dir=scene_dir,
        output_dir=output_dir,
        copy_originals=copy_originals,
        overwrite_output=overwrite_output,
    )

    frame_ids, frame_decisions = collect_scene_frame_decisions(
        scene_dir,
        max_frames=max_frames,
        augment_enabled=augment_enabled,
    )
    if not augment_enabled:
        save_json(
            {
                "scene_dir": str(scene_dir),
                "output_dir": str(output_dir),
                "scene_split": scene_split,
                "augmentation_enabled": False,
                "seed": seed,
                "copy_originals": copy_originals,
                "input_frame_count": len(frame_ids),
                "eligible_frame_count": 0,
                "generated_augmented_frames": 0,
                "generated_flip_frames": 0,
                "generated_color_only_frames": 0,
                "skipped_frames": len(frame_ids),
                "point_cloud_included": False,
                "manifest": [],
                "notes": [
                    "This camera-only export does not copy or generate point_cloud files.",
                    "Scenes in the val split are copied without any offline augmentation.",
                ],
            },
            output_dir / "augmentation_manifest.json",
        )

        print(f"Input frames considered: {len(frame_ids)}")
        print(f"Scene split: {scene_split} (augmentation disabled)")
        print("Eligible frames (target/long-tail): 0")
        print(f"Frames without augmentation: {len(frame_ids)}")
        print("Augmented flip frames generated: 0")
        print("Augmented color-only frames generated: 0")
        print("Augmented frames generated: 0")
        print("Point clouds copied/generated: 0")
        print(f"Saved to: {output_dir}")

        return {
            "scene_name": scene_dir.name,
            "scene_dir": str(scene_dir),
            "output_dir": str(output_dir),
            "scene_split": scene_split,
            "augmentation_enabled": False,
            "input_frame_count": len(frame_ids),
            "eligible_frame_count": 0,
            "generated_augmented_frames": 0,
            "generated_flip_frames": 0,
            "generated_color_only_frames": 0,
            "skipped_frames": len(frame_ids),
        }

    used_numeric_ids = {int(frame_id) for frame_id in frame_ids if frame_id.isdigit()}
    manifest: List[Dict[str, Any]] = []
    generated = 0
    flip_generated = 0
    color_only_generated = 0
    skipped = 0

    for frame_id in frame_ids:
        decision = frame_decisions.get(frame_id)
        color_only_count = int(color_only_counts.get(frame_id, 0)) if decision is not None else 0
        new_frame_ids = allocate_numeric_augmented_ids(
            frame_id=frame_id,
            count=1 + color_only_count,
            used_ids=used_numeric_ids,
        )

        source_images = load_frame_images(scene_dir, frame_id)
        image_widths = build_image_widths(source_images)
        label_data, camera_config, position_data = load_frame_metadata(scene_dir, frame_id)
        frame_class_counts = dict(sorted(collect_object_counts(label_data).items()))

        if decision is None:
            skipped += 1
            flip_trigger = "all_frame_flip"
            flip_profile_name = "identity"
            flip_canonical_classes = list(sorted(frame_class_counts))
            flip_class_counts = frame_class_counts
            flip_target_tail_count = 0
            flip_long_tail_count = 0
        else:
            flip_trigger = decision.trigger
            flip_profile_name = decision.profile
            flip_canonical_classes = list(decision.canonical_classes)
            flip_class_counts = decision.class_counts
            flip_target_tail_count = decision.target_tail_count
            flip_long_tail_count = decision.long_tail_count

        flip_frame_id = new_frame_ids[0]
        flip_plan = make_identity_photometric_plan(flip_profile_name)
        save_augmented_images(
            source_images=source_images,
            output_dir=output_dir,
            new_frame_id=flip_frame_id,
            do_flip=True,
            photometric_plan=flip_plan,
            seed=seed,
        )
        save_augmented_metadata_files(
            output_dir=output_dir,
            new_frame_id=flip_frame_id,
            label_data=label_data,
            camera_config=camera_config,
            position_data=position_data,
            do_flip=True,
            image_widths=image_widths,
        )
        manifest.append(
            {
                "new_frame_id": flip_frame_id,
                "source_frame_id": frame_id,
                "augment_index": 0,
                "augmentation_type": "flip",
                "trigger": flip_trigger,
                "profile": flip_profile_name,
                "canonical_classes": flip_canonical_classes,
                "class_counts": flip_class_counts,
                "target_tail_count": flip_target_tail_count,
                "long_tail_count": flip_long_tail_count,
                "flip_horizontal": True,
                "photometric_plan": flip_plan,
            }
        )
        generated += 1
        flip_generated += 1

        for color_idx in range(color_only_count):
            new_frame_id = new_frame_ids[color_idx + 1]
            aug_rng = stable_rng(seed, frame_id, "color_only", str(color_idx))
            intensity_name = choose_color_intensity(decision, color_idx)
            photometric_plan = sample_photometric_plan(
                aug_rng, decision.profile, intensity_name
            )

            save_augmented_images(
                source_images=source_images,
                output_dir=output_dir,
                new_frame_id=new_frame_id,
                do_flip=False,
                photometric_plan=photometric_plan,
                seed=seed,
            )
            save_augmented_metadata_files(
                output_dir=output_dir,
                new_frame_id=new_frame_id,
                label_data=label_data,
                camera_config=camera_config,
                position_data=position_data,
                do_flip=False,
                image_widths=image_widths,
            )
            manifest.append(
                {
                    "new_frame_id": new_frame_id,
                    "source_frame_id": frame_id,
                    "augment_index": color_idx + 1,
                    "augmentation_type": "color_only",
                    "trigger": decision.trigger,
                    "profile": decision.profile,
                    "canonical_classes": list(decision.canonical_classes),
                    "class_counts": decision.class_counts,
                    "target_tail_count": decision.target_tail_count,
                    "long_tail_count": decision.long_tail_count,
                    "flip_horizontal": False,
                    "photometric_plan": photometric_plan,
                }
            )
            generated += 1
            color_only_generated += 1

    save_json(
        {
            "scene_dir": str(scene_dir),
            "output_dir": str(output_dir),
            "scene_split": scene_split,
            "augmentation_enabled": True,
            "seed": seed,
            "copy_originals": copy_originals,
            "input_frame_count": len(frame_ids),
            "eligible_frame_count": len(frame_decisions),
            "generated_augmented_frames": generated,
            "generated_flip_frames": flip_generated,
            "generated_color_only_frames": color_only_generated,
            "skipped_frames": skipped,
            "point_cloud_included": False,
            "manifest": manifest,
            "notes": [
                "This camera-only export does not copy or generate point_cloud files.",
                "Every train-split input frame always gets exactly one horizontal flip sample.",
                "Only frames containing pedestrian/bicycle/motor or long-tail classes receive extra color-only samples.",
                "Color-only samples only modify image appearance and keep label/calibration geometry unchanged.",
                "position JSON is copied with only the top-level name updated.",
                "camera_config is mirrored only when horizontal flip is applied.",
                "2D_bbox is mirrored within the same image, without swapping camera folders.",
                "Augmented frame ids are new numeric ids allocated after each source frame id.",
            ],
        },
        output_dir / "augmentation_manifest.json",
    )

    print(f"Input frames considered: {len(frame_ids)}")
    print(f"Scene split: {scene_split} (augmentation enabled)")
    print(f"Eligible frames (target/long-tail): {len(frame_decisions)}")
    print(f"Frames without target/long-tail class (flip-only): {skipped}")
    print(f"Augmented flip frames generated: {flip_generated}")
    print(f"Augmented color-only frames generated: {color_only_generated}")
    print(f"Augmented frames generated: {generated}")
    print("Point clouds copied/generated: 0")
    print(f"Saved to: {output_dir}")

    return {
        "scene_name": scene_dir.name,
        "scene_dir": str(scene_dir),
        "output_dir": str(output_dir),
        "scene_split": scene_split,
        "augmentation_enabled": True,
        "input_frame_count": len(frame_ids),
        "eligible_frame_count": len(frame_decisions),
        "generated_augmented_frames": generated,
        "generated_flip_frames": flip_generated,
        "generated_color_only_frames": color_only_generated,
        "skipped_frames": skipped,
    }


def run(
    scene_root: Path,
    output_root: Path,
    seed: int,
    copy_originals: bool,
    max_frames: Optional[int],
    overwrite_output: bool,
    target_total_frames: int,
) -> None:
    scene_dirs = collect_scene_dirs(scene_root)
    if not scene_dirs:
        raise FileNotFoundError(
            f"No valid scene directories were found under: {scene_root}"
        )

    split_scenes = DEFAULT_TRAIN_VAL_SPLIT
    scene_dirs_by_split = partition_scene_dirs_by_split(
        scene_root=scene_root,
        scene_dirs=scene_dirs,
        split_scenes=split_scenes,
    )
    train_scene_dirs = scene_dirs_by_split["train"]
    val_scene_dirs = scene_dirs_by_split["val"]
    ordered_scene_dirs = [*train_scene_dirs, *val_scene_dirs]
    if not ordered_scene_dirs:
        raise FileNotFoundError(
            f"No scenes listed in DEFAULT_TRAIN_VAL_SPLIT were found under: {scene_root}"
        )

    scene_split_by_name = {
        scene_dir.name: "train" for scene_dir in train_scene_dirs
    }
    scene_split_by_name.update(
        {scene_dir.name: "val" for scene_dir in val_scene_dirs}
    )

    output_root.mkdir(parents=True, exist_ok=True)
    summary: List[Dict[str, Any]] = []
    total_input_frames = 0
    total_generated_frames = 0
    total_flip_frames = 0
    total_color_only_frames = 0
    scene_frame_decisions: Dict[str, Dict[str, FrameDecision]] = {}
    total_eligible_frames = 0
    train_input_frames = 0
    val_input_frames = 0
    scene_input_frame_counts: Dict[str, int] = {}

    scene_scan_tasks = [
        (
            scene_dir,
            max_frames,
            scene_split_by_name[scene_dir.name] == "train",
        )
        for scene_dir in ordered_scene_dirs
    ]

    with ProcessPoolExecutor(max_workers=PARALLEL_WORKERS) as executor:
        for scene_dir, frame_ids, frame_decisions in executor.map(
            _collect_scene_frame_decisions_worker, scene_scan_tasks
        ):
            scene_name = scene_dir.name
            scene_input_frame_counts[scene_name] = len(frame_ids)
            total_input_frames += len(frame_ids)
            if scene_split_by_name[scene_name] == "train":
                train_input_frames += len(frame_ids)
                total_eligible_frames += len(frame_decisions)
                scene_frame_decisions[scene_name] = frame_decisions
            else:
                val_input_frames += len(frame_ids)

        print("Using built-in DEFAULT_TRAIN_VAL_SPLIT")
        print(f"Train scenes found: {len(train_scene_dirs)}")
        print(f"Val scenes found: {len(val_scene_dirs)}")
        print("Val scenes (augmentation disabled):")
        for scene_dir in val_scene_dirs:
            scene_name = scene_dir.name
            print(f"  {scene_name}: {scene_input_frame_counts.get(scene_name, 0)} frames")
        print(f"Val source frames traversed (no augmentation): {val_input_frames}")
        print(f"Train source frames traversed: {train_input_frames}")
        print(f"Train source frames that will be augmented: {train_input_frames}")

        target_generated_frames = max(
            0,
            int(target_total_frames) - (total_input_frames if copy_originals else 0),
        )
        target_color_only_frames = max(0, target_generated_frames - train_input_frames)
        scene_color_only_counts = plan_scene_color_only_counts(
            scene_frame_decisions, target_color_only_frames=target_color_only_frames
        )

        scene_tasks = []
        for index, scene_dir in enumerate(ordered_scene_dirs, start=1):
            scene_name = scene_dir.name
            scene_split = scene_split_by_name[scene_name]
            scene_output_dir = output_root / scene_dir.name
            print(
                f"[{index}/{len(ordered_scene_dirs)}] Processing {scene_split} scene: "
                f"{scene_name}"
            )
            scene_tasks.append(
                (
                    scene_dir,
                    scene_output_dir,
                    seed,
                    copy_originals,
                    max_frames,
                    overwrite_output,
                    scene_color_only_counts.get(scene_name, {}),
                    scene_split == "train",
                    scene_split,
                )
            )

        for scene_summary in executor.map(_run_single_scene_worker, scene_tasks):
            summary.append(scene_summary)
            total_generated_frames += int(scene_summary["generated_augmented_frames"])
            total_flip_frames += int(scene_summary["generated_flip_frames"])
            total_color_only_frames += int(scene_summary["generated_color_only_frames"])

    save_json(
        {
            "scene_root": str(scene_root),
            "output_root": str(output_root),
            "train_val_split_source": "DEFAULT_TRAIN_VAL_SPLIT",
            "scene_count": len(summary),
            "train_scene_count": len(train_scene_dirs),
            "val_scene_count": len(val_scene_dirs),
            "total_input_frames": total_input_frames,
            "train_input_frames": train_input_frames,
            "val_input_frames": val_input_frames,
            "eligible_frame_count": total_eligible_frames,
            "target_total_frames": int(target_total_frames),
            "total_generated_augmented_frames": total_generated_frames,
            "total_generated_flip_frames": total_flip_frames,
            "total_generated_color_only_frames": total_color_only_frames,
            "target_color_only_frames": target_color_only_frames,
            "total_output_frames": total_generated_frames + (total_input_frames if copy_originals else 0),
            "point_cloud_included": False,
            "scenes": summary,
        },
        output_root / "dataset_augmentation_summary.json",
    )
    print(f"Processed scenes: {len(summary)}")
    print(f"Total input frames: {total_input_frames}")
    print(f"Train input frames: {train_input_frames}")
    print(f"Val input frames: {val_input_frames}")
    print(f"Eligible frames: {total_eligible_frames}")
    print(f"Target total frames: {int(target_total_frames)}")
    print(f"Target color-only frames: {target_color_only_frames}")
    print(f"Total generated flip frames: {total_flip_frames}")
    print(f"Total generated color-only frames: {total_color_only_frames}")
    print(f"Total generated augmented frames: {total_generated_frames}")
    print(
        "Total output frames: "
        f"{total_generated_frames + (total_input_frames if copy_originals else 0)}"
    )
    print(f"Dataset output root: {output_root}")


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(
        "--scene-dir",
        type=Path,
        default=Path("fisheye_2wdata"),
        help="Input dataset root containing multiple scene directories, or a single scene directory.",
    )
    parser.add_argument(
        "--output-dir",
        type=Path,
        default=Path("fisheye_data_aug"),
        help="Output dataset root. Each scene keeps its original scene name under this directory.",
    )
    parser.add_argument(
        "--seed",
        type=int,
        default=20260415,
        help="Global random seed used to make augmentation deterministic.",
    )
    parser.add_argument(
        "--target-total-frames",
        type=int,
        default=DEFAULT_TARGET_TOTAL_FRAMES,
        help=(
            "Target total number of frames after augmentation. "
            "When originals are copied, this includes original frames."
        ),
    )
    parser.add_argument(
        "--no-copy-originals",
        action="store_true",
        help="Only export augmented samples instead of copying original camera/label files first.",
    )
    parser.add_argument(
        "--max-frames",
        type=int,
        default=None,
        help="Optional debug limit for how many source frames to scan.",
    )
    parser.add_argument(
        "--overwrite-output",
        action="store_true",
        help="Delete and recreate --output-dir if it already exists.",
    )
    return parser.parse_args()


def main() -> None:
    args = parse_args()
    #从 Octopus 环境变量读取路径
    scene_root = Path(os.environ["OCTPS_DATASET_DIR"]) / "fisheye_2wdata"
    # scene_root = Path(os.environ["TARGET_RESULT_DIR"]) / "fisheye_2wdata"
    output_root = Path(os.environ["TARGET_RESULT_DIR"]) / "fisheye_data_aug"
    output_root.mkdir(parents=True, exist_ok=True)

    print("使用输入路径:", scene_root)
    print("使用输出路径:", output_root)
    run(
        scene_root=scene_root,
        output_root=output_root,
        # scene_root=args.scene_dir,
        # output_root=args.output_dir,
        seed=args.seed,
        copy_originals=not args.no_copy_originals,
        max_frames=args.max_frames,
        overwrite_output=args.overwrite_output,
        target_total_frames=args.target_total_frames,
    )


if __name__ == "__main__":
    main()
